# Server
PORT=3000
NODE_ENV=development

# JWT — REQUIRED: replace with any long random string before running
JWT_SECRET=CHANGE_ME_to_a_long_random_secret
JWT_EXPIRES_IN=24h

# Database
DB_PATH=./data/finledger.db

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
